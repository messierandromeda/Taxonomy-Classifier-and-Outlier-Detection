

Land taxonomy classifier:
    Get CLC code from coords
    Compare to LLM CLC code
    If same -> high likelyhood it is the same
    If different, consider length of input field:
        If short -> take official CLC code
        Else take LLM CLC code

    Testing:
    Experiment diff models. Compare to most powerful (affordable) model.
    Experiment with order of prompt (reasoning before assigning code)
    Experiment with adding plant name
    Experiment with adding structured fields to prompt
    Return top 3 outputs
    Combine input fields together instead of appending